import 'package:flutter/material.dart';

class NestsScreen extends StatelessWidget {
  const NestsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ninhos ativos'),
      ),
      body: const Center(
        child: Text('Esta funcionalidade ainda não foi implementada.'),
      ),
    );
  }
}